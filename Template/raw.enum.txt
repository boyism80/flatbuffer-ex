namespace {{ enum.scope.namespace | array.add 'raw' | array.each @cs_mapped_kwd | array.join '.' }};

enum {{ enum.name }} : {{ enum.type }}
{
{{~ for value in enum.values ~}}
	{{ value }}{{ if !for.last }},{{ end }}
{{~ end ~}}
}